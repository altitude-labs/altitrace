# HyperEVM Configuration, this should be a rpc that supports debug and tracing rpc endpoints
# for HyperEVM, see the nanoreth node https://github.com/hl-archive-node/nanoreth
RPC_URL=http://localhost:8545