{
  "name": "@altitrace/monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "book"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "lint:check": "turbo run lint:check",
    "fmt:check": "turbo run fmt:check",
    "fmt": "turbo run fmt",
    "type-check": "turbo run type-check",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.2.7",
  "dependencies": {},
  "devDependencies": {
    "@biomejs/biome": "2.0.6",
    "turbo": "^2.5.5"
  }
}